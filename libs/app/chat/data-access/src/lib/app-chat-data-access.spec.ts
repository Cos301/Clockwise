import { appChatDataAccess } from './app-chat-data-access';

describe('appChatDataAccess', () => {
  it('should work', () => {
    expect(appChatDataAccess()).toEqual('app-chat-data-access');
  });
});
