import { apiPostsDataAccess } from './api-posts-data-access';

describe('apiPostsDataAccess', () => {
  it('should work', () => {
    expect(apiPostsDataAccess()).toEqual('api-posts-data-access');
  });
});
